<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :activity %><br />
    <%= f.text_area :activity %>
  </div>
   <div class="field">
    <%= f.label :tag_list, "Tag (seperated by commas)" %><br />
    <%= f.text_area :tag_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

